{
  "name": "tokenpie",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "optimize": "node scripts/optimize-images.js",
    "vault-data": "node scripts/generate-vault-data.js",
    "apply-sets": "node scripts/apply-sets.js",
    "check-vault-images": "node scripts/check-vault-images.js",
    "check-set-zips": "node scripts/check-set-zips.js",
    "build-zips": "node scripts/build-set-zips.js",
    "set-zips-data": "node scripts/generate-set-zips-data.js",
    "build": "npm run optimize && npm run vault-data && npm run build-zips && npm run set-zips-data",
    "test": "vitest run"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "vitest": "^4.1.10"
  }
}
